<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asiantech.realmforandroid">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".MyApp"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BaseRealmActivity"></activity>
        <activity android:name=".json.JsonActivity"></activity>
        <activity android:name=".migration.MigrationActivity"></activity>
        <activity android:name=".thread.ThreadActivity"></activity>
        <activity android:name=".basic.BasicActivity"></activity>
    </application>

</manifest>